#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pii = pair<int, int>;
#define rep(i,a,b) for(int i=a;i<b;i++)
#define rrep(i,a,b) for(int i=a;i>=b;i--)

int main() {
    // cin の入力元をinput.txtに変更する
    ifstream in("input.txt");
    cin.rdbuf(in.rdbuf());
}